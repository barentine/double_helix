visgui:
    - double_helix.vis_modules.DH_mappings
    
# a list of fully qualified import paths for dh5view plugins
dsviewer:
    - double_helix.image_modules.DH_calibration
    
# a list of fully qualified import paths for recipe modules
recipes:
    - double_helix.recipes.DH_mappings

# a list of fully qualified import paths for fitfactories
fit_factories:
    # - double_helix.FitFactories.DoubleHelixFit_Theta
    # - double_helix.FitFactories.DoubleHelixFit_SepGuess
    - double_helix.DoubleGaussFit